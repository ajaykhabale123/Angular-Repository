// we can create array in diff ways

let arr1 : boolean [] = []; // true

let arr2 : boolean [] = new Array (); // true

let arr3 : boolean [] = Array () ; // true

let arr4 : Array <boolean> = [] ;

let arr5 : Array <boolean> = Array();

let arr6 : Array <boolean> = new Array ();

let arr14 = Array <boolean>();

// You can access them using index

console.log(arr1[0]);

// add eleents in array using push

// Types of array - Homogenous, Heterogenous 

var homoArr : Array<string> = ["Angular","JavaScript" ];

var heteroArr : Array <any> = ["HTML", 102, true];

// ArrayOf Object . 
